from setuptools import setup, find_packages
setup(
	name = "ascentti-diabetes",
	version = "0.1",
	packages = find_packages() 
	) 
# find_packages looks for all packages under the current folder